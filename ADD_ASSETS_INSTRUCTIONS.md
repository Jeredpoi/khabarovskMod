# Инструкция по добавлению файлов в Assets релиза

Для BetterDiscord плагинов есть **два способа** добавления файлов:

## 🌐 Способ 1: Через веб-интерфейс GitHub (Самый простой!)

### Шаги:

1. **Откройте страницу редактирования релиза:**
   - Перейдите: https://github.com/Jeredpoi/khabarovskMod/releases/edit/v0.0.1
   - Или нажмите на релиз → кнопка "Edit" (карандаш) в правом верхнем углу

2. **Добавьте файлы в Assets:**
   - Прокрутите страницу вниз до секции **"Attach binaries by dropping them here or selecting them"**
   - Перетащите файлы `khabarovskMod.plugin.js` и `khabarovskMod.config.json` в эту область
   - ИЛИ нажмите "selecting them" и выберите файлы

3. **Сохраните:**
   - Нажмите кнопку **"Update release"** внизу страницы

4. **Готово!** Файлы появятся в секции Assets релиза

---

## 💻 Способ 2: Через GitHub CLI (Автоматизация)

### Если установлен GitHub CLI:

```bash
cd "c:\Users\B-ZONE\Documents\Visual Studio\another_Projects"

# Загрузка плагина
gh release upload v0.0.1 khabarovskMod.plugin.js

# Загрузка конфига
gh release upload v0.0.1 khabarovskMod.config.json
```

### Установка GitHub CLI (если не установлен):

1. Скачайте: https://cli.github.com/
2. Установите
3. Выполните авторизацию: `gh auth login`
4. Затем используйте команды выше

---

## 📋 Способ 3: Через PowerShell скрипт (с токеном доступа)

Если у вас есть GitHub Personal Access Token:

1. **Создайте токен:**
   - GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
   - Создайте новый токен с правами `repo`

2. **Запустите скрипт:**
   ```powershell
   cd "c:\Users\B-ZONE\Documents\Visual Studio\another_Projects"
   .\upload_release_assets.ps1 -Token YOUR_GITHUB_TOKEN
   ```

---

## ⚠️ Важно для BetterDiscord!

### Для автоматической установки через BetterDiscord:

BetterDiscord использует метаданные `@source` для автоматической установки плагина. В вашем плагине уже настроено:

```javascript
 * @source https://github.com/Jeredpoi/khabarovskMod/raw/main/khabarovskMod.plugin.js
```

Эта ссылка должна указывать на **raw файл** (не blob), чтобы BetterDiscord мог напрямую скачать код. Это уже правильно настроено! ✅

### Зачем нужны Assets?

- **Assets** - для пользователей, которые хотят скачать файлы вручную
- **@source** - для автоматической установки через BetterDiscord

Оба варианта полезны! Assets удобны для тех, кто хочет скачать конкретную версию релиза, а @source - для автоматической установки последней версии.

---

## 🔗 Полезные ссылки:

- Релизы: https://github.com/Jeredpoi/khabarovskMod/releases
- Редактирование релиза: https://github.com/Jeredpoi/khabarovskMod/releases/edit/v0.0.1
- Raw файл плагина: https://github.com/Jeredpoi/khabarovskMod/raw/main/khabarovskMod.plugin.js

---

## 📝 Рекомендация:

**Для BetterDiscord плагинов лучше всего:**
1. ✅ Использовать `@source` с raw ссылкой (уже настроено)
2. ✅ Добавить файлы в Assets через веб-интерфейс (для ручной загрузки)
3. ✅ Обновлять README с инструкциями по установке

Тогда у пользователей будет выбор: автоматическая установка через BetterDiscord или ручная загрузка из Assets.
