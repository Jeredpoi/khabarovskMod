# 📘 Руководство по настройке khabarovskMod.config.json

## 📋 Описание

Файл `khabarovskMod.config.json` позволяет настроить все аспекты работы плагина под ваши нужды. Файл создается автоматически при первом запуске плагина в папке `%AppData%\BetterDiscord\plugins\`.

---

## 📁 Структура конфига

### 1. Форматы сообщений (`messageFormats`)

#### `withText`
**Описание:** Формат сообщения, которое отправляется автоматически в чат при выборе наказаний из категории `punishmentsWithText`.

**Пример:**
```json
"withText": "<@{userId}> +{punishment} по пункту {ruleId} правил"
```

**Доступные переменные:**
- `{userId}` - ID пользователя (будет заменен на `<@userId>`)
- `{punishment}` - Название наказания
- `{ruleId}` - ID пункта правила (например, "2.15")

#### `onlyMention`
**Описание:** Формат только упоминания пользователя (используется для наказаний, которые не входят ни в одну категорию).

**Пример:**
```json
"onlyMention": "<@{userId}>"
```

#### `commands` - Форматы команд

##### `warn`
**Описание:** Команда для выдачи предупреждения. Используется для наказания "Предупреждение".

**Пример:**
```json
"warn": "/warn user:<@{userId}> reason:{ruleId}"
```

##### `mute`
**Описание:** Команда для мута на 90 минут. Используется для наказания "Мут 90 минут".

**Пример:**
```json
"mute": "/mute user:<@{userId}> time:90 reason:{ruleId}"
```

##### `ban`
**Описание:** Команда для временного бана. Используется для наказания "Бан 7-15 дней". Время указывается вручную при вставке команды.

**Пример:**
```json
"ban": "/ban user:<@{userId}> time: reason:{ruleId}"
```

##### `permban`
**Описание:** Команда для перманентного бана (365 дней). Используется для наказания "Перманентная блокировка".

**Пример:**
```json
"permban": "/ban user:<@{userId}> time:365 reason:{ruleId}"
```

##### `user`
**Описание:** Команда для проверки пользователя. Используется в разделе "Инструменты модерации" → "Проверка пользователя".

**Пример:**
```json
"user": "/user user:<@{userId}>"
```

##### `punish`
**Описание:** Команда для просмотра истории наказаний. Используется в разделе "Инструменты модерации" → "Punish".

**Пример:**
```json
"punish": "/punish user:<@{userId}>"
```

---

### 2. Категории наказаний

#### `punishmentsWithText`
**Описание:** Список наказаний, которые автоматически отправляют сообщение в текущий канал.

**Поведение:** При выборе такого наказания, сообщение отправляется автоматически в чат.

**Пример:**
```json
"punishmentsWithText": [
    "Устное предупреждение"
]
```

#### `punishmentsWithTextAndCopy`
**Описание:** Список наказаний, которые отправляют сообщение И копируют команду в буфер обмена.

**Поведение:** При выборе такого наказания:
1. Сообщение отправляется автоматически в чат
2. Команда копируется в буфер обмена для вставки

**Пример:**
```json
"punishmentsWithTextAndCopy": [
    "Предупреждение"
]
```

#### `punishmentsWithCopy`
**Описание:** Список наказаний, которые только копируют команду в буфер обмена.

**Поведение:** При выборе такого наказания команда копируется в буфер обмена, но сообщение НЕ отправляется автоматически.

**Пример:**
```json
"punishmentsWithCopy": [
    "Мут 90 минут",
    "Бан 7-15 дней",
    "Перманентная блокировка"
]
```

---

## 📝 Полный пример конфига

```json
{
    "messageFormats": {
        "withText": "<@{userId}> +{punishment} по пункту {ruleId} правил",
        "onlyMention": "<@{userId}>",
        "commands": {
            "warn": "/warn user:<@{userId}> reason:{ruleId}",
            "mute": "/mute user:<@{userId}> time:90 reason:{ruleId}",
            "ban": "/ban user:<@{userId}> time: reason:{ruleId}",
            "permban": "/ban user:<@{userId}> time:365 reason:{ruleId}",
            "user": "/user user:<@{userId}>",
            "punish": "/punish user:<@{userId}>"
        }
    },
    "punishmentsWithText": [
        "Устное предупреждение"
    ],
    "punishmentsWithTextAndCopy": [
        "Предупреждение"
    ],
    "punishmentsWithCopy": [
        "Мут 90 минут",
        "Бан 7-15 дней",
        "Перманентная блокировка"
    ]
}
```

---

## 🔧 Как настроить

### Способ 1: Через панель настроек BetterDiscord
1. Откройте BetterDiscord → Плагины
2. Найдите `khabarovskMod`
3. Нажмите на иконку ⚙️ (Настройки)
4. Измените нужные параметры
5. Нажмите "💾 Сохранить настройки"

### Способ 2: Редактирование файла напрямую
1. Откройте папку: `%AppData%\BetterDiscord\plugins\`
2. Найдите файл `khabarovskMod.config.json`
3. Откройте его в любом текстовом редакторе
4. Внесите изменения
5. Сохраните файл
6. Перезагрузите плагин в BetterDiscord

---

## ⚠️ Важные замечания

1. **JSON синтаксис:** Убедитесь, что файл содержит валидный JSON (правильные кавычки, запятые, скобки)
2. **Переменные:** В форматах сообщений и команд используйте переменные `{userId}`, `{punishment}`, `{ruleId}` - они будут заменены автоматически
3. **Названия наказаний:** Названия наказаний в списках должны точно совпадать с теми, что указаны в плагине
4. **Резервная копия:** Перед изменением конфига сделайте его копию

---

## 🆘 Решение проблем

### Конфиг не загружается
- Проверьте синтаксис JSON (используйте валидатор JSON)
- Убедитесь, что все кавычки закрыты
- Проверьте, что запятые расставлены правильно

### Настройки не применяются
- Перезагрузите плагин (отключите и включите)
- Проверьте, что файл сохранен в правильной кодировке (UTF-8)
- Убедитесь, что названия наказаний совпадают точно

### Плагин не работает после изменения конфига
- Удалите файл `khabarovskMod.config.json`
- Перезагрузите плагин (он создаст новый файл с настройками по умолчанию)
- Внесите изменения заново

---

## 📞 Поддержка

Если у вас возникли проблемы с настройкой конфига:
1. Проверьте этот файл
2. Проверьте раздел Issues на GitHub
3. Создайте новый Issue с описанием проблемы

---

**Последнее обновление:** Версия 0.0.6
